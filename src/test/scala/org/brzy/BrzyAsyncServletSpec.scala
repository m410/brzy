package org.brzy

import org.scalatest.matchers.ShouldMatchers
import org.scalatest.WordSpec


class BrzyAsyncServletSpec extends WordSpec with ShouldMatchers {

  "BrzyAsyncServlet" should {
    "call async" in {
      assert(false)
    }
  }
}
