#
# implement persistence for the cassandra nosql db
#
name: brzy-security
org: org.brzy
version: "0.3-SNAPSHOT"
config_class: org.brzy.security.SecurityModConfig
resource_class: org.brzy.security.SecurityModProvider

authority_entity: Person
user_entity:
user_entity_name:
user_entity_pass:
algorithm:
default_path:
authority_field:

dependencies:
  - {conf: compile, org: org.brzy, name: brzy-security, rev: "0.3-SNAPSHOT", transitive: false}

web_xml:
  - filter:
    filter-name: SecurityFilter
    filter-class: org.brzy.security.SecurityFilter
  - filter-mapping:
    filter-name: SecurityFilter
    url-pattern: /*
  - security-constraint:
    - web-resource-collection:
        web-resource-name: Reasaurs
        url-pattern: /*
    - auth-constraint:
        role-name: Toppy
    - user-data-constraint:
        transport-guarantee: NONE
  - login-config:
    - auth-method: FORM
    - form-login-config:
        form-login-page: /auth/login.html
        form-error-page: /auth/login-error.html

