package org.brzy.mod.devmode

import org.brzy.fab.mod.RuntimeMod

/**
 * Document Me..
 *
 * @author Michael Fortin
 */
class DevModeModConfig(override val map:Map[String,AnyRef]) extends RuntimeMod(map)
