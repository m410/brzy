package org.brzy.fab.conf

import org.scalatest.junit.JUnitSuite
import org.junit.Test


class RepositoryTest extends JUnitSuite {
  @Test def testIt = {
    assert(true)
  }
  
  @Test def testMergeBaseConfs = {
    assert(true)
  }
}