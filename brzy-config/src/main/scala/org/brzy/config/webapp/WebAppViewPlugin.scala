package org.brzy.plugin

/**
 * Document Me..
 * 
 * @author Michael Fortin
 * @version $Id: $
 */

abstract class WebAppViewPlugin(map: Map[String, AnyRef]) extends Plugin(map) {
  val fileExtension = ""
}