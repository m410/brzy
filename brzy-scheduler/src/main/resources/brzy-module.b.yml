#
#
name: brzy-scheduler
org: org.brzy
version: "0.3"
config_class: org.brzy.scheduler.SchedulerModConfig
resource_class: org.brzy.scheduler.SchedulerModProvider

dependencies:
  - {conf: compile, org: org.brzy, name: brzy-scheduler, rev: "0.3", transitive: false}
